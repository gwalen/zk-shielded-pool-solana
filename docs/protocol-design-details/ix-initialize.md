## Initialize

This function is called when the contract is deployed to set up the initial parameters.

Effect of this instruction:
- set up the Root Registry with the initial state for root ring buffer and IMT tree
